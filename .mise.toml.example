# Example mise configuration for installing dz-skills CLI tools
#
# Copy this to your project root or global config:
#   cp .mise.toml.example .mise.toml
#   mise install

[tools]
# Install markdown-surgeon (md) and worklog (wl) from GitHub releases
# Uses github backend to download pre-compiled binaries
"github:dohzya/dz-skills" = "md-v0.4.0"
"github:dohzya/dz-skills" = "wl-v0.4.0"

# Note: mise will automatically detect and download the correct binary
# for your platform (darwin-arm64, linux-x86_64, etc.)
